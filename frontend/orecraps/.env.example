# Solana Network Configuration
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# RPC Endpoint - Get your key from https://helius.dev
NEXT_PUBLIC_RPC_ENDPOINT=https://devnet.helius-rpc.com/?api-key=YOUR_API_KEY_HERE

# Admin keypair configuration
# REQUIRED: Must set ADMIN_KEYPAIR_PATH for CLI-based operations
# This should point to a valid Solana keypair JSON file
ADMIN_KEYPAIR_PATH=/path/to/your/keypair.json

# For production: Use base58 encoded secret key instead of file paths
# ADMIN_KEYPAIR=base58-encoded-secret-key-here

# Admin API Authentication Token (required for admin endpoints)
# Generate with: openssl rand -base64 32
ADMIN_API_TOKEN=your-secure-random-token-here

# Entropy Seed Storage (for localnet testing)
SEED_STORAGE_DIR=/secure/path/to/seeds
# Optional AES-256 encryption key (64 hex characters = 32 bytes)
# Generate with: openssl rand -hex 32
# SEED_ENCRYPTION_KEY=your-64-character-hex-key-here

# Test keypair seed (for localnet testing only)
# Generate with: openssl rand -base64 32
# TEST_KEYPAIR_SEED=base64-encoded-32-byte-seed
